import Hooks from './Hooks'; // Adjust the path based on your file structure
import StateAndProps from './StateAndProps'; // Adjust the path based on your file structure

const App = () => {
  return (
    <div>
      <Hooks />
      <StateAndProps />
    </div>
  );
}

export default App;
